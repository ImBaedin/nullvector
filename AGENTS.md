After making code changes, always run the relevant linter and formatter for the affected package/app before finishing. This repo has had avoidable whitespace and indentation regressions in JSX, and the linter/formatter step is required to catch them. Run `lint:fix` and then `format`
